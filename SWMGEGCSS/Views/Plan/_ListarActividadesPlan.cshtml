@model SWMGEGCSS.Models.GestionarPlanProyectoViewModel
@{
    var listaTotalActividades = Model.List_Actividades;
    var listaPlanActividad = Model.List_Actividades_planeadas;
    var listaPlanes = Model.plans;
    //List<int> cantidadDisp = (List<int>)Session["ListaCantidadPermitida"];
    int count = 0;
    //int count1 = 0;
}

<div class="container-fluid" id="tablaActividades">
    <form form id="form">
        @if (listaTotalActividades != null)
        {
            <h3 class="text-center">Actividades por Planificar</h3>
            <div class="row mt-4 pl-2">
                <div class="col-12 col-sm-11 col-md-11">
                    <div class="table-responsive">
                        <table class="table table-bordered">
                            <thead class="bg-success">
                                <tr class="text-center">
                                    <th scope="col">#</th>
                                    <th scope="col">Actividad</th>
                                    <th scope="col">Descripcion</th>
                                    <th scope="col">Obligatorio</th>
                                    @*<th scope="col">Cantidad Disp</th>*@
                                    <th scope="col">Funciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in listaTotalActividades)
                                {
                                    //var cd = cantidadDisp[count];
                                    count = count + 1;
                                    //var plan_id = listaPlanes.plan_id;
                                    //data-id-act="@item.act_id" data-id-plan="@plan_id" data-url="@Url.Action("_ModalAgregarActividadesPlanificadas", "Plan")" data-id-target="#RegistrarActPlan"
                                <tr class="text-center">
                                    <th scope="row">@count</th>
                                    <td>@item.act_nombre</td>
                                    <td>@item.act_descripcion </td>
                                    @if (item.act_obligatorio == true)
                                        {
                                            <td>Si</td>
                                        }
                                        else
                                        {
                                            <td>No</td>
                                        }
                                    <td>
                                        @*@{
                                        if (cd != 0)
                                        {*@
                                        <button class="btn btn-success btnModal mov_button1" type="button" data-id-act="@item.act_id" data-url="@Url.Action("_ModalRegistrarActividadesPlanificadas","Plan")" data-id-target="#RegistrarActividad">
                                            <i class="fa fa-plus-circle"></i>
                                            Agregar a la Planificacion
                                        </button>
                                        @*}
                                            else
                                            {
                                                <label style="border: 1px solid #C00" class="text-center form-control">
                                                    <Font size="3" color="red">
                                                        Cantidad Max. Alcanzada
                                                    </Font>
                                                </label>
                                            }
                                        }*@
                                    </td>
                                </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <!--LISTA DE TAREAS PLANIFICADAS-->
            @*<div class="row mt-4 pl-2">
                @Html.Partial("_ListarActividadesPorPlanear", Model)
            </div>*@
            <div class="row mt-4 pl-2">
                <div class="col-12 col-sm-3 col-md-3">

                </div>
            </div>
            @Html.Action("_ModalRegistrarActividadesPlanificadas", "Plan")
        }
    </form>
</div>

